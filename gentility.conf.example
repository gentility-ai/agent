# Gentility AI Agent Configuration
# Copy this file to /etc/gentility.conf and edit as needed

# REQUIRED: Your Gentility AI access token
# Get this from your Gentility AI dashboard
GENTILITY_TOKEN=your_token_here

# OPTIONAL: Server URL (default: wss://api.gentility.ai)
# Use wss:// for production, ws:// for development
#SERVER_URL=wss://api.gentility.ai

# OPTIONAL: Agent nickname (default: hostname)
# This helps identify your agent in the dashboard
#NICKNAME=my-server-01

# OPTIONAL: Environment (default: prod)
# Options: prod, staging, dev
#ENVIRONMENT=prod

# OPTIONAL: Debug logging (uncomment to enable)
# This will log all WebSocket message traffic
#DEBUG=true

# SECURITY: Agent activation settings
# When set, the agent requires activation for sensitive operations (DB queries, execution)

# OPTIONAL: Security mode (default: none)
# Options: none, password, totp
#SECURITY_MODE=none

# OPTIONAL: Security password (if SECURITY_MODE=password)
# Plain text password for agent activation
#SECURITY_PASSWORD=your_secure_password

# OPTIONAL: TOTP secret (if SECURITY_MODE=totp) 
# Base32 encoded TOTP secret for two-factor authentication
#SECURITY_TOTP_SECRET=JBSWY3DPEHPK3PXP

# OPTIONAL: Activation timeout in seconds (default: 1800 = 30 minutes)
# How long the agent stays active after authentication
#SECURITY_ACTIVATION_TIMEOUT=1800

# OPTIONAL: Extendable activation (default: true)
# If true: each sensitive command resets the timeout (rolling window)
# If false: hard cutoff after timeout regardless of activity
#SECURITY_EXTENDABLE=true

# PROMISCUOUS MODE: Allow server to export security config for replication
# Uses the same credentials as normal security mode

# OPTIONAL: Enable promiscuous mode (default: true)
# Allows server to export security configuration to replicate to other agents
#PROMISCUOUS_ENABLED=true

# OPTIONAL: Promiscuous authentication mode (default: password)
# Which credential type required for promiscuous operations: password, totp
# Uses the same SECURITY_PASSWORD or SECURITY_TOTP_SECRET from above
#PROMISCUOUS_AUTH_MODE=password